<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
    
	    <!-- Brand and toggle get grouped for better mobile display -->
	    <div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand randopedia-logo-text" {{action goToIndex}}>Randopedia</a>
			{{view App.SearchTextField valueBinding="controllers.search.query" class="searchbox"}}
	    </div>
	    
	    <!-- Collect the nav links, forms, and other content for toggling -->
	    <div class="collapse navbar-collapse main-menu-container" id="bs-example-navbar-collapse-1">
			
           {{#if controllers.login.isLoggedIn}}
	           <a {{action topbarMenuLink "tour.new"}}>Add new tour</a>
	           <a {{action topbarMenuLink "mytours"}}>View my tours</a>
           {{else}}
               {{#if controllers.login.isLoggingIn}}
                    <div class="header-login-text top-bar-menu-nolink">Logging in...</div>   
               {{else}}
                    <a data-toggle="modal" data-target="#loginViewModalId">Login</a>
               {{/if}}
           {{/if}}
           
           <a {{action topbarMenuLink "area-browse"}}>Browse areas</a>
           <a {{action topbarMenuLink "about"}} data-toggle="collapse" data-target=".nav-collapse">About Randopedia</a>
       
	    </div>

    </div>
</nav>

<div class="modal fade" id="loginViewModalId" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    {{view App.LoginModalView}}
</div>