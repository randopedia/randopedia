
{{#if liteTours}}
    <div id="full-screen-map-container" class="full-screen">
        {{browse-tourmap store=store tours=liteTours zoomLevel=currentMapZoomLevel mapCenter=currentMapCenter mapTypeId=currentMapTypeId tour=tourForMapView zoomChanged="mapZoomChanged" centerChanged="mapCenterChanged" mapTypeIdChanged="mapTypeIdChanged"}}
    </div>
{{else}}
    <div class="loading">Loading map...</div>
{{/if}}




