
import { displayDateTime } from 'randopedia-web-2/helpers/display-date-time';
import { module, test } from 'qunit';

module('Unit | Helper | display date time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = displayDateTime([42]);
  assert.ok(result);
});

