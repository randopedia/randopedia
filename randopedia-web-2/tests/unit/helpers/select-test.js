import { select } from 'randopedia-web-2/helpers/select';
import { module, test } from 'qunit';

module('Unit | Helper | select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = select([42]);
  assert.ok(result);
});
