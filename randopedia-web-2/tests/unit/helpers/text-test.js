import { text } from 'randopedia-web-2/helpers/text';
import { module, test } from 'qunit';

module('Unit | Helper | text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = text([42]);
  assert.ok(result);
});
