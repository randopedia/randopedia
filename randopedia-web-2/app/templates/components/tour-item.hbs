<div class="tour-item">  
    <div class="item-panel">
        <h4> <a {{action "openTour"}}>{{tour.name}}</a></h4>
        <div class="item-props">    
            {{resolveSelectName 'Grades' tour.grade}} | 
            {{tour.timingMin}}-{{tour.timingMax}}h | 
            {{tour.elevationGain}}m &uarr; {{tour.elevationLoss}}m &darr;
        </div>
    </div>
</div>